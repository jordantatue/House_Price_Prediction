[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-housing"
version = "0.1.0"

# Package discovery: include the top-level `src` package
[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

